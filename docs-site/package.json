{
  "name": "@offlinejs/docs-site",
  "private": true,
  "description": "Static OfflineJS documentation site (zero runtime dependencies).",
  "scripts": {
    "build": "test -f out/index.html",
    "docs:build": "node build.cjs"
  }
}
