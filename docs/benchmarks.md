# OfflineJS Benchmarks

Generated: 2026-07-20T07:16:02.466Z · Node v22.14.0

| Adapter | Metric | Dataset | Duration | Throughput |
| --- | --- | ---: | ---: | ---: |
| memory | writes | 5,000 | 13.77ms | 363,165 ops/s |
| memory | batch-writes | 5,000 | 13.23ms | 378,006 ops/s |
| memory | find | 5,000 | 2.36ms | 42,419 ops/s |
| memory | indexed-find | 5,000 | 0.34ms | 294,035 ops/s |
| indexeddb | writes | 5,000 | 268.15ms | 18,647 ops/s |
| indexeddb | batch-writes | 5,000 | 92.23ms | 54,210 ops/s |
| indexeddb | find | 5,000 | 31.04ms | 3,222 ops/s |
| indexeddb | indexed-find | 5,000 | 15.74ms | 6,352 ops/s |
| sqlite | writes | 5,000 | 15.37ms | 325,333 ops/s |
| sqlite | batch-writes | 5,000 | 17.76ms | 281,537 ops/s |
| sqlite | find | 5,000 | 3.31ms | 1,509,827 ops/s |
| sqlite | indexed-find | 5,000 | 2.88ms | 34,704 ops/s |

## Notes

- Writes measure sequential storage.set throughput.
- Batch writes use storage.setMany when available (one durable batch path).
- Find measures filtered+sorted+limited query latency; records = page size returned.
- Indexed find creates a secondary index on `group`, then times equality lookup.
- Prefer durationMs / percentiles for finds — ops/s on page size is not rows scanned.


## How to reproduce

```bash
pnpm bench
# optional: OFFLINEJS_BENCH_RECORDS=50000 pnpm bench
```

This report is generated by exercising the real `@offlinejs/storage-*` adapters through
`@offlinejs/benchmarks` (`runPerformanceReport`). Scores below are from this machine run.

## What was optimized

Recent performance work targets hot paths the suite measures:

- IndexedDB / memory / SQLite / OPFS `setMany` — one durable batch transaction
- Memory finds clone only the returned page; index lookups hydrate only the requested window
- Shared `applyQuery` skips redundant filter/sort copies
- SQLite equality + order/limit pushdown via `json_extract` when engine-safe
- Queue/sync status-filtered due reads + concurrent push batches
- Collection `paginate` uses a single storage read
